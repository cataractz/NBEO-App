import React from "react";
import ReactDOM from "react-dom/client";
import NBEOStudyApp from "../nbeo-app.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NBEOStudyApp />
  </React.StrictMode>
);
